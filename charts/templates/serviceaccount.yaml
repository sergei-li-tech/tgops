{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "tgops.fullname" .) }}
  labels:
    {{- include "tgops.labels" . | nindent 4 }}
{{- end }}